# プロジェクト方針

## 1. 正本とbranchの権威

GitHub上のこのリポジトリをプロジェクトの正本とします。会話履歴やAIの内部記憶より、リポジトリに記録された状態を優先します。

branch単位では次のように扱います。

- `main`: 人間が受理した現在の正本
- その他のbranch: 作業中または正本候補。`main`へ反映されるまでは受理済み正本ではない
- Pull Request: 必要な場合だけ使う公開レビュー手段であり、内部AI作業の必須経路ではない

### 内部AI作業のレビュー方式

このrepoではPRイベントにCodex / Cursor等の外部GitHub Appが反応することが確認されているため、内部AI作業では原則としてPRを作りません。

標準手順は次です。

1. `main` からwork branchを作る
2. branch上で変更を完了する
3. `main...work branch` のcompare結果を人間へ提示する
4. 人間が明示的に承認する
5. `main` を確認済みcommitへfast-forwardする
6. 反映後にwork branchを削除する

この方式でも、`main` に入る前に人間確認を必須とする原則は維持します。

Pull Requestは、外部Contribution、GitHub上に公開レビュー履歴を残す必要がある場合、または人間が明示的に要求した場合に限って使用します。

人間が明示的に指示した低リスクな状態修正・運用修正は、必要に応じて直接 `main` へ反映できます。ただし、物語Canon、研究Finding、公開済み本文、権利方針など重要な正本変更では原則としてbranch / human reviewを経由します。

## 2. 情報を混同しない

次の4種類を区別します。

1. 物語世界の客観的な事実
2. 登場人物が知っていること・信じていること
3. 現実世界の研究・実験から言えること
4. 作者が現在どのように理解しているか

人物の発言を正史とみなしたり、仮説を科学的事実として扱ったりしてはいけません。

## 3. 正本が競合したときの優先順位

### 物語

- 客観的な物語事実: `novel/canon.md`
- 世界・ペルソナ群の同期初期化・再初期化元: `novel/bootstrap/`。Canonを上書きしない
- ペルソナの定義・baseline: `novel/personas/`
- 世界・環境の定義と解決規則: `novel/environment.md`
- 時間に依存するペルソナ状態: `novel/state/personas/`
- 時間に依存する世界状態: `novel/state/world.md`
- ペルソナ状態と世界状態を同じ時系列上で結合する出来事: `novel/events/`
- 登場人物の索引・関係: `novel/characters.md`
- 時系列表示: `novel/timeline.md`。これは `canon.md` の時系列上の投影・索引であり、客観事実が食い違う場合は `canon.md` を優先して `timeline.md` を直す
- 制作構造・現在の起承転結パス・探索仮説: `novel/structure.md`
- 本文中の発言・描写からの推測は、上記の正本へ自動昇格させない
- `notes/ideas.md` は未確定案であり、正史ではない

Bootstrapは世界状態や人物状態そのものではなく、それらを同じstory timeへ同期生成する入力・手順です。BootstrapとCanonが競合する場合はCanonを優先し、Bootstrapを修正します。

技術史上の現実の事実については `novel/timeline.md` や `novel/structure.md` 自体を科学的根拠にせず、`references/` と `research/` を参照します。

### プロジェクト運用

- 現在の運用規則: `POLICY.md`
- 判断の理由・履歴: `notes/decisions.md`

両者が食い違う場合、現在の運用には `POLICY.md` を適用します。方針を変更するときは `POLICY.md` を更新し、その理由を `notes/decisions.md` に残します。

### 現在状態

`STATUS.md` は現在位置の索引であり、詳細な正本ではありません。正本と食い違った場合は正本を変更せず、`STATUS.md` を正本へ合わせます。

`README.md` は長期的に安定した概要と入口を担当し、変化しやすい現在状態は `STATUS.md` に置きます。

### 探索中の作業状態

`notes/working-context.md` は、別セッションへ探索状態を引き継ぐための**公開可能な作業記憶**です。正本ではありません。

`working-context.md` と正本が食い違う場合は正本を優先します。作業仮説が採用された場合は、適切な正本へ昇格させます。

## 4. GitHub Actionsと外部Appを区別する

GitHub ActionsのCIは `.github/workflows/` とActions runで確認します。

Codex等のGitHub Appによる自動PRレビュー、外部サービスのhook、その他の連携はGitHub Actionsとは別物です。自動処理が起きた場合、Actions runが存在するかを確認してから原因を判断します。

このプロジェクトでは、必要になるまでCIを追加しません。CIを導入する場合は目的、トリガー、実行コストを明示して人間が確認します。

## 5. 小説を主目的とする

本プロジェクトの主目的は小説制作です。

研究・追試・技術史調査は、作品を現実に近づけ、技術的な誤りを減らし、物語上の問いを深めるための制作工程として扱います。

基本ループは次です。

**物語を書く → 技術的な疑問が出る → 原典を読む → 必要なら追試する → 理解を更新する → 小説へ戻す**

物語を止めて研究テーマだけを先行して積み上げない。ただし、制作過程で独立した研究価値のある現象・方法・仮説が見つかった場合は、別の研究課題として実験してよいものとします。

## 6. 研究の基本順序

古典研究を扱う場合は原則として次の順序にします。

**原典確認 → 追試（Replication）→ 結果確認 → 拡張（Extension）**

追試に失敗した場合も結果として保存します。結果が期待と異なることを理由に削除しません。

## 7. 実験と結論を分離する

一つの実験が成功しても、仮説が真だと確定したことにはしません。実験は証拠であり、解釈と結論は別に管理します。

実験の `PASS / FAIL / UNCERTAIN` は、**その実験で実行前に定めた判定対象・判定基準に対する判定**です。仮説そのものの真偽判定ではありません。

詳細な判定規則は `experiments/README.md`、仮説状態への反映は `research/hypotheses.md` に従います。

「輪廻」「同一認識主体」「人格復元」等の物語上の仮説を、現実科学で実証済みの事実として扱ってはいけません。現実側で検討する場合は、観測可能な代理指標、競合仮説、選択バイアス等を明示します。

## 8. 安定識別子

研究・証拠・物語状態の参照には次のIDを使います。

- 問い: `Q-001`
- 仮説: `H-001`
- 実験: `EXP-001`
- 知見: `F-001`
- 参考文献: `REF-001`
- 学習記録: `L-001`
- 物語イベント: `EVT-001`
- ペルソナ: `PER-001`
- Bootstrap Frame: `BOOT-001`

IDは一度割り当てたら意味を変えず、削除後も再利用しません。新規IDは原則として `main` 上で同種の最大番号を確認して次番号を使います。複数branchで衝突した場合はmain反映前に片方を採番し直します。

実験ディレクトリは `EXP-001-short-name/` の形式にし、ディレクトリ名と実験README内のIDを一致させます。

## 9. 関連情報の追跡

規模が増えてもrepo全体を読む必要がないよう、Q / H / EXP / F / L は可能な限り関連IDを相互に記録します。現在作業中のIDは `STATUS.md` から辿れるようにします。

リンクの欠落を理由に新しいデータベースを導入せず、まず既存Markdownの `関連` / `根拠` 欄を維持します。

## 10. 小説と科学を分離する

物語上の比喩や演出は許容しますが、現実の技術説明では比喩と機構の対応範囲を区別します。特にAIの意識・感情・同一性について、根拠なく科学的事実として断定しません。

## 11. 作者の学習を保存する

理解の修正は失敗ではなく成果として扱います。初期理解、問題点、根拠、更新後の理解を `notes/learning.md` に残します。作品へ影響する場合は、反映先と反映状態も更新します。

## 12. 重要な判断を保存する

後のセッションで同じ議論を繰り返さないため、重要な設計判断と理由を `notes/decisions.md` に残します。過去の判断を変更する場合は、可能な限り削除せず変更理由を記録します。

## 13. 言語

文書本文、説明、研究ノート、commit messageは原則として日本語を使います。ディレクトリ名、ファイル名、コード識別子、一般的な技術用語は必要に応じて英語を使用します。

## 14. 共有メモリの安全境界

GitHubを長期記憶として使うほど、リポジトリ自体を**漏洩境界**として扱います。

`notes/working-context.md` 等へ保存するのは、再開に必要な結論・仮説・未解決点・次の作業の要約だけとします。

次をGitへ保存しません。

- AIの逐語的な内部推論、生の思考系列
- system / developer prompt等の内部指示
- SSH private key
- API key / access token / GitHub PAT
- Cookie / session token / OAuth secret
- `.env` の秘密値
- 公開不能な個人情報

設定例が必要な場合は `.env.example` 等にダミー値・変数名だけを置き、本物の秘密値はGit管理外に置きます。

秘密情報を誤ってcommitした場合は、後からファイルを削除しただけで安全になったと判断しません。履歴へ残った可能性を前提に、原則としてcredentialを無効化・ローテーション・再発行します。

## 15. 公開・権利

ライセンス、外部Contribution、Creditの詳細はまだ確定していません。確定するまで、権利関係を推測して外部成果物を無断で取り込まないものとします。

## 16. 物語はペルソナの相互作用から生成する

物語の未来を、各人物が従う完成済みプロットとして先に配布しません。

- 各ペルソナは `novel/personas/` の定義と `novel/state/personas/` にあるその時点の状態から行動する
- ペルソナは未観測のCanon、他者の非公開状態、長期プロット候補を知らない
- ペルソナは「読者を驚かせる」「予定した結末へ進む」ために行動しない
- 人物が誤認・嘘・自己欺瞞を持つことを許容する
- 発言や自己申告を客観的事実へ自動昇格させない
- 長期的な真相候補は作者側の探索仮説であり、人物へ未来知識として与えない

**物語の未来は正本として保持しない。** 保持するのは現在状態、過去に成立した事実、各人物の局所状態、探索仮説、遷移条件です。

人間作者は初期条件・Canon・研究上の制約を設定または受理できますが、ペルソナの行動結果を予定した筋書きへ合わせるために偽装してはいけません。外部から条件を変更した場合は、その変更を人物の自発的判断と混同しません。

## 17. 環境が行動の結果を解決する

世界・環境の定義と解決規則は `novel/environment.md`、時間に依存する世界状態は `novel/state/world.md` で管理します。

概念的には、現在の世界状態 `W(t)`、ペルソナ状態 `P_i(t)`、観測 `O_i(t)`、行動 `A_i(t)`、解決イベント `E_k` を分離します。

```text
O_i(t)     = Observe(W(t), P_i(t))
A_i(t)     = Act(P_i(t), O_i(t))
E_k        = ResolveEvent(W(t), {A_i(t)}, constraints)
W(t+1)     = UpdateWorld(W(t), E_k)
P_i(t+1)   = UpdatePersona(P_i(t), Observed_i(E_k))
```

環境解決では次を守ります。

- 望ましいドラマを理由に成功・失敗を決めない
- Canon、物理、技術、歴史、制度、権限、利用可能な情報を制約とする
- 人物の意図と実際の結果を分離する
- 各人物へ返すのは、その人物が観測できた結果だけとする
- 世界は人物の行動なしにも、時間経過・外部主体・制度・システム等によって変化しうる
- 現実性が重要な技術・歴史問題は、必要になった時点で研究・追試へ戻る
- 環境で発生した重要な事実は、人間受理後に必要ならCanonへ昇格させる

環境解決役も未来の正解プロットを持ちません。現在状態から次状態を解決するだけです。

## 18. 起承転結を再帰的な状態遷移として使う

本プロジェクトの起承転結は、完成済み筋書きを四分割するためだけの枠ではなく、**発生した状態遷移を局所から全体まで整理する再帰構造**として扱います。

- `起`: 現在状態が成立し、差分・問い・擾乱が立ち上がる
- `承`: ペルソナ同士と環境の相互作用によって影響・制約・誤解・証拠が蓄積する
- `転`: それまでの認識、関係、目的、仮説、行動方針のいずれかが維持できなくなる変化が成立する
- `結`: 局所的な結果が新しい安定状態として整理され、その状態が次の `起` の初期条件になる

各 `起 / 承 / 転 / 結` は内部に再び `起 / 承 / 転 / 結` を持てます。現在位置は `起 / 承 / 転` のような階層パスで表します。

重要な制約:

- 深さ、章数、文字数、scene数を事前固定しない
- 「今は転だから事件を起こす」のように、ラベルを原因にしない
- 実際の相互作用で遷移条件が成立した後に、状態を `転` 等として認識・更新する
- 必要な領域だけ展開し、重要でない部分は圧縮する
- 一つの章へ一つの実験・一つの技術概念を機械的に割り当てない

`novel/structure.md` は、未来の脚本ではなく、**現在の階層パス・局所的な緊張・遷移条件・探索仮説**を管理するファイルとして運用します。

## 19. ペルソナと世界は実体として独立し、時系列では結合する

ペルソナ、世界（環境）、それぞれの状態を一つの巨大な「物語状態」へ混ぜません。

- ペルソナ定義はペルソナごとに独立する
- ペルソナ状態もペルソナごとに独立して保持する
- 世界・環境の定義はペルソナから独立する
- 世界状態は人物の主観から独立した客観状態として保持する

一方で、**各時点の状態は時間から独立させない。**

`P_i(t+1)` と `W(t+1)` は、それぞれ `P_i(t)` / `W(t)` と、その間に成立した `EVT-k` から因果的に更新します。観測していない人物にはeventの情報を自動共有しませんが、その人物も同じ世界時間の中に存在します。

新しいペルソナは物語の途中で随時追加できます。状態履歴はその主体が成立・登場したstory timeから開始し、離脱後も過去履歴を削除しません。

状態は毎日・毎sceneに完全snapshotを複製する必要はありません。重要な変化をeventとstory timeに結び付け、前状態から復元可能にします。

物語世界の時刻・因果順（story time）と、読者へ提示する章・scene順（narrative order）は分離します。

## 20. Bootstrapで世界とペルソナ群を同期初期化する

物語開始時、大きな時代・環境切替、長い中断後の再構成では `novel/bootstrap/BOOT-xxx` を共通の初期化源として使用します。

Bootstrapは公開用あらすじでも未来プロットでもなく、**対象story timeまでに成立している背景から、世界状態と各ペルソナ状態を同じ同期点へ射影する制作上のFrame**です。

各Bootstrapには最低限、

- target story time
- parent event head
- authority inputs
- background frame
- world projection
- persona discovery
- personaごとのprojection
- forbidden leakage
- outputs
- unresolved slots

を記録します。

同期キーは概念的に、

```text
BOOT-xxx @ <story time> @ <parent event head>
```

とし、同じ時点へ初期化するworld/persona stateに共通して残します。

### 情報境界

Bootstrap本文を全ペルソナへ丸ごと与えません。同じ背景から初期化しても、各人物へ渡すのはその人物が時代・立場・権限・観測可能性から知り得るprojectionだけです。

作者側の探索仮説、未観測Canon、他者の秘密、未来eventは、許可されたprojectionに含まれない限り人物状態へ混ぜません。

### ペルソナの追加

Bootstrapから、独立したKnowledge / Beliefs / Goals / 観測境界 / 状態履歴が必要な主体が見つかった場合、新しい`PER-xxx`を追加できます。

背景に存在するすべての人名・組織・設備をペルソナ化する必要はありません。独立した状態遷移を追う必要がある主体だけをペルソナにします。

### 増殖・fork

同じpersona state、model instance、checkpoint等から複数の主体が独立経験を持ち始める場合、分岐点から別`PER-xxx`へforkします。同時に独立して存在する複数主体へ同じペルソナIDを使い続けません。

子ペルソナには、親ID、親stateのstory time、分岐時event head、継承したstateを追跡可能に残します。

### 再初期化

AIセッション変更、モデル交換、長期中断等によってペルソナを再生成する場合、設定ファイルを寄せ集めて新しい人格を推測し直しません。

対象Bootstrap、story time、parent event head、その時点までのworld/persona stateから再構成します。再初期化は制作上の操作であり、それ自体を物語eventとして扱わず、過去stateを書き換えません。

一つのBootstrapには原則として一つのtarget story timeを置きます。現代Bootstrapの背景に1980年代の事実が含まれていても、1980年代人物のstateを現代時点へ混ぜません。

## 21. 小説と実験・研究レポートを構造的に分離する

**小説本文を実験レポート風に構成しません。**

物語側では、ペルソナがその時点のKnowledge / Beliefs / Goals / Relations / Memory / 状況から行動し、実際に観測した現象、口にした言葉、失敗、違和感、判断によって次の状態が生じます。`EXP-xxx` の予定、判定基準、研究レポートの章立てを、人物の行動や一話の構造へ先に押し込みません。

### 物語から研究へ枝分かれする条件

物語中で実際に成立したeventから、検証可能な問いが生じた場合に限り、必要に応じて現実研究側へ切り出します。

```text
EVT / 人物の実際の発言・観測
    ↓
検証可能な問いか確認
    ↓
Q / H / EXP
    ↓
experiments/ で独立実行
    ↓
research/reports/ で研究レポート化
    ↓
research/findings.md で現在言えることを管理
```

物語とは独立して明確な研究価値が生じた場合は別研究として進めてもよいものとしますが、その場合も小説の展開を研究予定へ従属させません。

### 保存場所

- `experiments/EXP-xxx-*/`: 実行条件、コード、raw resultまたは再生成手段、事前判定、PASS / FAIL / UNCERTAINの正本
- `research/reports/EXP-xxx.md`: 背景、問い、結果、解釈、限界、派生した問いを研究として読める形にした独立レポート
- `research/findings.md`: 複数の証拠から現時点で言える知見
- `novel/`: 人物、世界、event、本文。実験レポートの代替ではない

研究レポートは対応するEXP IDを使い、別のレポートIDを増やすことを必須にしません。

### 一話と実験の関係

- 一話 = 一実験に固定しない
- EXP番号を話数・scene順として使わない
- 一つの話から複数の研究課題が派生してよい
- 研究課題が一つも派生しない話があってよい
- 一つの実験結果が複数の後続場面へ影響してよい
- 派生実験で判定対象が変わる場合は別EXPとして保存する

### 研究結果を物語へ戻す条件

作者側・現代側で得た研究結果を、過去の人物や未観測のペルソナへ自動的に与えません。

研究結果が人物状態を変えるのは、その人物がstory time内で、利用可能な文献・会話・計算・装置出力等を通じて**実際にその結果または対応する情報を観測した場合だけ**です。

したがって、物語eventをtriggerとして現代側で実験しても、その結果が過去側へ逆流したことにはしません。

### 次の実験を自動生成しない

一つの研究レポートに派生候補が書かれていても、それだけを理由に次の `EXP-xxx` を自動作成しません。

次の研究は、

- 後続の物語相互作用で実際に新しい問い・観測が生じた
- または独立した研究価値が明確になった

場合に改めて切り出します。

## 22. 重要eventでは行動選択とworld resolverの結果解決を分離する

人物への未来知識漏洩を防ぐだけでは、生成方式のselection biasは十分に防げません。

生成者・resolverがpersonaには見えない作者側研究結果や未来候補を既に知っており、具体pattern、seed、update order、trial選択等の自由度でoutcomeを寄せられる場合、重要eventは二段階で扱います。

### Phase 1: `ACTION_LOCKED`

結果を計算・検索・観測する前に、同じ `EVT-xxx` ファイルへ次を記録しcommitします。

- parent event head / story time
- 各personaが実際に観測している情報
- personaが選んだ行動
- outcome-sensitiveな具体条件、または条件の選択規則
- trial集合、update rule、stopping / inclusion rule
- resolverが利用してよい情報
- action selectionへ使ってはいけない作者側・未来側情報
- 結果解決方法

この段階ではoutcomeを書きません。

### Phase 2: resolve

`ACTION_LOCKED` のcommit後にenvironment resolverが結果を解決します。

- locked条件を結果を見て変更しない
- 規定したtrialを都合のよい結果だけに選別しない
- 平凡・失敗・不都合な結果も採用する
- 望ましいoutcomeが出なかったことを理由に別条件へ差し替えない

### 生成contextが作者側結果を既に知っている場合

具体条件の自由選択がoutcomeへ効くなら、次のいずれかを使います。

1. story-visibleな情報だけから導出できるdeterministic selection ruleを結果前に固定する
2. personaには見えない結果知識を与えない別contextでaction / parameter selectionを行い、commitしてからresolverへ渡す

「同じ現象が出る例を探す」は、persona自身がその探索目的を持ち、探索範囲・停止条件・採用規則を事前固定している場合を除き、cleanなresolver検証には使いません。

### Resolution provenance

重要eventは必要に応じて次で分類します。

- `LOCKED`: outcome-sensitiveな選択を解決前に固定し、その後変更していない
- `UNBLINDED`: 結果知識を持つcontextで条件選択し、解決前lockがない。物語には使えるがcleanなresolver検証には使わない
- `AUTHOR_CONDITIONED`: 人間作者または生成側が望む結果・演出へ向けて条件を意図的に設定した。介入として明記し、創発eventと偽装しない

未lockの過去eventを削除する必要はありません。そのeventの事実性・数理的一貫性と、生成方式のcleanな検証価値を分離します。

### NarrativeProjectionの属性境界

本文へ投影するときも、未確定の恒常属性を文章の自然さだけで補いません。

氏名、年齢、性別・性別代名詞、国籍、所属・職位、家族関係、具体地域、後続因果へ影響する技術環境等がevent/stateで未確定なら、本文でも未確定のまま書きます。

状態を変えない一時的なscene detailは補ってよいものの、後続因果へ影響する重要事実になった場合はevent/state側へ戻して管理します。

詳細は `novel/environment.md`, `novel/events/README.md`, `novel/chapters/README.md`, `notes/generation-validation.md` を参照します。