# プロジェクト方針

## 1. 正本とbranchの権威

GitHub上のこのリポジトリをプロジェクトの正本とします。会話履歴やAIの内部記憶より、リポジトリに記録された状態を優先します。

branch単位では次のように扱います。

- `main`: 人間が受理した現在の正本
- その他のbranch / Pull Request: 作業中または正本候補。`main`へmergeされるまでは受理済み正本ではない

PRを監査するときはそのbranchを「候補状態」として読んでよいですが、既存の正本を変更したことにはしません。

## 2. 情報を混同しない

次の4種類を区別します。

1. 物語世界の客観的な事実
2. 登場人物が知っていること・信じていること
3. 現実世界の研究・実験から言えること
4. 作者が現在どのように理解しているか

人物の発言を正史とみなしたり、仮説を科学的事実として扱ったりしてはいけません。

## 3. 正本が競合したときの優先順位

### 物語

- 客観的な物語事実: `novel/canon.md`
- 時系列表示: `novel/timeline.md`。これは `canon.md` の時系列上の投影・索引であり、客観事実が食い違う場合は `canon.md` を優先して `timeline.md` を直す
- 登場人物の認識: `novel/characters.md`
- 本文中の発言・描写からの推測は、上記の正本へ自動昇格させない
- `notes/ideas.md` は未確定案であり、正史ではない

技術史上の現実の事実については `novel/timeline.md` 自体を科学的根拠にせず、`references/` と `research/` を参照します。

### プロジェクト運用

- 現在の運用規則: `POLICY.md`
- 判断の理由・履歴: `notes/decisions.md`

両者が食い違う場合、現在の運用には `POLICY.md` を適用します。方針を変更するときは `POLICY.md` を更新し、その理由を `notes/decisions.md` に残します。

### 現在状態

`STATUS.md` は現在位置の索引であり、詳細な正本ではありません。正本と食い違った場合は正本を変更せず、`STATUS.md` を正本へ合わせます。

`README.md` は長期的に安定した概要と入口を担当し、変化しやすい現在状態は `STATUS.md` に置きます。

## 4. 研究の基本順序

古典研究を扱う場合は原則として次の順序にします。

**原典確認 → 追試（Replication）→ 結果確認 → 拡張（Extension）**

追試に失敗した場合も結果として保存します。結果が期待と異なることを理由に削除しません。

## 5. 実験と結論を分離する

一つの実験が成功しても、仮説が真だと確定したことにはしません。実験は証拠であり、解釈と結論は別に管理します。

実験の `PASS / FAIL / UNCERTAIN` は、**その実験で実行前に定めた判定対象・判定基準に対する判定**です。仮説そのものの真偽判定ではありません。

詳細な判定規則は `experiments/README.md`、仮説状態への反映は `research/hypotheses.md` に従います。

## 6. 安定識別子

研究・証拠の参照には次のIDを使います。

- 問い: `Q-001`
- 仮説: `H-001`
- 実験: `EXP-001`
- 知見: `F-001`
- 参考文献: `REF-001`
- 学習記録: `L-001`

IDは一度割り当てたら意味を変えず、削除後も再利用しません。新規IDは原則として `main` 上で同種の最大番号を確認して次番号を使います。複数PRで衝突した場合はmerge前に片方を採番し直します。

実験ディレクトリは `EXP-001-short-name/` の形式にし、ディレクトリ名と実験README内のIDを一致させます。

## 7. 関連情報の追跡

規模が増えてもrepo全体を読む必要がないよう、Q / H / EXP / F / L は可能な限り関連IDを相互に記録します。現在作業中のIDは `STATUS.md` から辿れるようにします。

リンクの欠落を理由に新しいデータベースを導入せず、まず既存Markdownの `関連` / `根拠` 欄を維持します。

## 8. 小説と科学を分離する

物語上の比喩や演出は許容しますが、現実の技術説明では比喩と機構の対応範囲を区別します。特にAIの意識・感情・同一性について、根拠なく科学的事実として断定しません。

## 9. 作者の学習を保存する

理解の修正は失敗ではなく成果として扱います。初期理解、問題点、根拠、更新後の理解を `notes/learning.md` に残します。作品へ影響する場合は、反映先と反映状態も記録します。

## 10. 重要な判断を保存する

後のセッションで同じ議論を繰り返さないため、重要な設計判断と理由を `notes/decisions.md` に残します。過去の判断を変更する場合は、可能な限り削除せず変更理由を記録します。

## 11. 言語

文書本文、説明、研究ノート、commit messageは原則として日本語を使います。ディレクトリ名、ファイル名、コード識別子、一般的な技術用語は必要に応じて英語を使用します。

## 12. 公開・権利

ライセンス、外部Contribution、Creditの詳細はまだ確定していません。確定するまで、権利関係を推測して外部成果物を無断で取り込まないものとします。
